#include<bits/stdc++.h>
#define ll long long
#define ld long double
#define w(t) long long t ; cin>>t ; while(t--)
#define f(i,a,b) for(ll i=a;i<b;i++)
using namespace std;
int main(){
    w(t){
        ll n;
        cin>>n;
        vector<string> name;
        f(i,0,n){
        string x;
        cin>>x;
        name.push_back(x);
        }
    }
    return 0;
}