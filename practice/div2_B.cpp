#include<bits/stdc++.h>
#define N  "NO\n"
#define Y "YES\n"
#define ll long long
// #define max(a,b,c) max(max(a,b),c) 

#define w(t) long long t ; cin>>t ; while(t--)
#define f(i,a,b) for(ll i=a;i<b;i++)
#define fi(itr,v) for(auto itr=v.begin();itr!=v.end();itr++)
#define IOS ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
using namespace std;

// int main(){
//     ll n;
//     double l,two=2;
//     cin>>n>>l;
//     double a[n];
//     f(i,0,n) cin >> a[i];
//     sort(a,a+n);
//     double max_diff = INT_MIN;
//     f(i,0,n-1){
//         double diff = a[i+1] - a[i];
//         max_diff = max(diff,max_diff); 
//     }
   
//     f(i,0,n-1){
//         double diff = a[i+1] - a[i];
//         if(diff == max_diff) {cout << i << endl;} 
//     }
//     f(i,0,n) cout << i << " " << a[i] << endl;
//     cout << endl;

//     double ans = max( max( a[0] , (l-a[n-1]) ) , ( max_diff/two ) );
//     cout << ans ;
// }
int main(){
    
}